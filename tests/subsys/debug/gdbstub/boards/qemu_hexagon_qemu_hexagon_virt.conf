# Hexagon uses a custom gdbstub backend (UART polling via arch code)
CONFIG_GDBSTUB_SERIAL_BACKEND=n
CONFIG_GDBSTUB_CUSTOM_BACKEND=y

# Hexagon has 44 registers * 4 bytes * 2 (hex) = 352 bytes minimum
CONFIG_GDBSTUB_BUF_SZ=512
